log file = /dev/stdout
use chroot = yes
max connections = 4
strict modes = no

[qt]
path = /mnt/qt
comment = "Qt (only for upstream use, please use HTTP to download files)"
read only = true

[raspbian]
path = /mnt/raspbian
comment = "Raspbian (only for upstream use, please use HTTP to download files)"
read only = true
